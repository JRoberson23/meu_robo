name: Execução Diária do Robô 🤖

on:
  schedule:
    - cron: '0 10 * * *'  # Executa todo dia às 10:00 UTC
  workflow_dispatch:

jobs:
  executar:
    runs-on: ubuntu-latest  # CORRIGIDO: "lastest" → "latest"

    steps:
      - name: 📂Clonar Repositório
        uses: actions/checkout@v3
        
      - name: ⚙️Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
        
      - name: Instalar dependências
        run: pip install pandas
        
      - name: 📦Instalar dependências
        run: pip install -r requirements.txt
                
      - name: 🤖Executar robô
        run: python robo.py
        
      - name: 📝Commit do relatório
        run: | 
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add dados/relatorio.csv
          git commit -m "Atualização diária do relatório" || exit 0
          git push
